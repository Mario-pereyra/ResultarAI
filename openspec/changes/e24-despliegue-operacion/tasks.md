# Tasks — e24-despliegue-operacion

## 1. Docker Compose de instancia

- [ ] 1.1 Crear `deploy/Dockerfile.backend` (FastAPI, uv, usuario no-root) y `deploy/Dockerfile.frontend` (Next.js standalone, usuario no-root), ambos multi-stage. Verificación: ambas imágenes construyen y `docker inspect` muestra usuario ≠ root en el proceso principal (escenarios "usuario no-root"). `[modelo: sonnet]`
- [ ] 1.2 Crear `deploy/compose.yaml` con los servicios plataforma backend, frontend, Postgres, Langfuse (y sus dependencias propias) y Uptime Kuma, en una red interna Docker; imágenes de terceros referenciadas `imagen:tag@sha256:...`. Verificación: `docker compose config` resuelve sin errores y cada imagen de terceros declara digest (escenario "Cada imagen de terceros declara su digest"). `[modelo: sonnet]`
- [ ] 1.3 Declarar healthchecks por servicio (endpoint o comando, con intervalo/timeout/reintentos) y `depends_on: condition: service_healthy` en los dependientes. Verificación: con Postgres detenido a propósito, el backend no arranca; con todo sano, `docker compose ps` muestra todos `healthy` (escenarios del requirement "Healthchecks por servicio"). `[modelo: sonnet]`
- [ ] 1.4 Declarar volúmenes nombrados independientes para datos de Postgres y para el `storage_path` de adjuntos. Verificación: `docker compose down && up` preserva datos, y `docker volume ls` muestra los dos volúmenes separados (escenarios del requirement "Volúmenes persistentes"). `[modelo: sonnet]`
- [ ] 1.5 Configurar el reverse proxy como único servicio con `ports:` publicados, enrutando frontend y API; ningún otro servicio publica puertos. Verificación: `docker compose config` lista `ports:` solo en el proxy; desde fuera del host la conexión directa a Postgres/Langfuse/gateway falla (escenarios "Postgres inaccesible desde fuera de la red interna" y "El gateway LLM tampoco publica puerto"). `[modelo: sonnet]`

## 2. Configuración por entorno y seed inicial

- [ ] 2.1 Crear `deploy/.env.example` con TODAS las variables que el Compose y sus servicios referencian (Postgres, secreto de sesión, claves LLM, Langfuse, dominio/TLS, retención de backups), cada una comentada, sin secretos reales; agregar `.env` a `.gitignore`. Verificación: script de diff entre variables usadas en `compose.yaml` y entradas del ejemplo devuelve vacío (escenario "`.env.example` cubre todas las variables requeridas"). `[modelo: haiku]`
- [ ] 2.2 Crear el seed de configuración de instancia (`deploy/seed/`): archivo versionado con branding, retención de adjuntos, límites de attachments, idioma default, umbral de cuota y TTL de HITL, cargado por el comando de seed hacia la configuración de instancia de `d20`. Verificación: instancia limpia arranca con esos valores visibles en la consola de gobernanza (escenario "Instancia recién levantada tiene configuración de instancia poblada"). `[modelo: sonnet]`
- [ ] 2.3 Implementar el comando de seed idempotente: crea la cuenta Admin con contraseña temporal (hash Argon2id vía `d11`; la contraseña solo sale por stdout), carga y valida los manifiestos de fábrica con la validación fail-fast de `a02`, y deja skill/tool/workflow de ejemplo activos. Verificación: primer login del Admin fuerza wizard con TOTP; segundo `up` no re-siembra; un manifiesto corrupto aborta el seed (escenarios de los requirements de seed). `[modelo: sonnet]`

## 3. Backups y restauración

- [ ] 3.1 Crear el sidecar de backup programado: `pg_dump` diario + tar del volumen de adjuntos hacia un destino bind-mount del host distinto de los volúmenes de producción, con retención configurable por variables y purga automática. Verificación: forzar dos corridas manuales produce dos juegos de backup y la purga elimina los que exceden la retención (escenarios del requirement "Backup diario"). `[modelo: sonnet]`
- [ ] 3.2 Escribir `deploy/scripts/restore.sh` y el procedimiento de restauración paso a paso (incluye el caso degradado de restore sin volumen de adjuntos). Verificación: el documento cubre restore completo y parcial con los comandos exactos copiables. `[modelo: sonnet]`
- [ ] 3.3 Ejecutar una prueba de restore REAL: levantar instancia, generar datos (usuario, sesión con mensajes, adjunto), respaldar, destruir (`down -v`), restaurar y verificar login + sesiones + adjuntos intactos. Verificación: evidencia de la corrida (transcript o script automatizado en verde) adjunta al PR (escenario "Restore de backup deja la instancia funcional"). `[modelo: sonnet]`

## 4. Hardening

- [ ] 4.1 Configurar TLS en el reverse proxy (certificado y redirección HTTP→HTTPS) y security headers (`Strict-Transport-Security`, `X-Content-Type-Options`, framing, CSP compatible con Next.js). Verificación: `curl -I` sobre una ruta del frontend muestra todas las cabeceras y la petición HTTP plana redirige (escenarios del requirement "Security headers y TLS"). `[modelo: sonnet]`
- [ ] 4.2 Configurar rate limiting en el borde: umbral estricto para el endpoint de login (coherente con el bloqueo `ACCOUNT_LOCKED` de `d11`) y umbral general para la API, con respuesta `429`. Verificación: ráfaga de login sintética recibe `429`; tráfico de chat normal no (escenarios del requirement "Rate limiting"). `[modelo: sonnet]`
- [ ] 4.3 Configurar el límite de tamaño de request en proxy y backend derivado de la matriz de `d14` (máximo por tipo + 5 adjuntos por mensaje + overhead multipart, margen documentado). Verificación: subida legítima al límite mayor de `d14` pasa; request muy por encima recibe rechazo en el borde sin tocar el backend (escenarios del requirement "Límites de tamaño de request"). `[modelo: sonnet]`

## 5. Smoke test E2E

- [ ] 5.1 Diseñar el smoke test: contrato de los 7 pasos (login Admin + enrolamiento TOTP con `pyotp` → alta de usuario → turno de chat con `default_chat` → adjunto hasta `ready` → tool del MCP server de ejemplo vía skill con decisión del Policy Gate auditada → tarjeta HITL visible → workflow de ejemplo hasta resultado), criterios de éxito/timeout por paso y reporte de fallo con paso culpable. Verificación: documento de diseño del script revisado contra los escenarios del requirement "Smoke test E2E". `[modelo: opus]`
- [ ] 5.2 Implementar `deploy/scripts/smoke_test.py` (httpx contra la API pública) según el diseño de 5.1, con exit code ≠ 0 y paso culpable ante fallo. Verificación: corre en verde contra una instancia limpia recién sembrada; interrumpir un paso a propósito lo reporta y sale ≠ 0 (escenarios del requirement "Smoke test E2E"). `[modelo: sonnet]`
- [ ] 5.3 Crear el job opcional de CI que levanta el Compose efímero con proveedor LLM simulado (perfil de LiteLLM de `b05`), corre el smoke test y destruye el entorno siempre (éxito o fallo). Verificación: el job pasa en verde en un PR de prueba y no requiere credenciales reales (escenario "CI ejecuta el smoke test contra un Compose efímero"). `[modelo: sonnet]`

## 6. Runbook y cierre

- [ ] 6.1 Escribir `deploy/runbook.md`: kill-switch de `d20` como primer paso ante incidentes, fuentes de diagnóstico (vista de salud de `d19`, Langfuse, Uptime Kuma), procedimiento de restauración (referencia a 3.2), procedimiento de actualización de imágenes pinneadas y contactos de escalamiento; completa el placeholder operativo de `docs/06-seguridad-gobernanza.md`. Verificación: un operador que solo lee el runbook puede ejecutar kill-switch, diagnóstico y restore sin buscar en otro documento (escenarios del requirement "Runbook de incidentes"). `[modelo: sonnet]`
- [ ] 6.2 Documentar el despliegue de primera instancia en el README de `deploy/` (orden del Migration Plan: `.env` → `up` → seed → primer login TOTP → smoke test → backups) y actualizar `CLAUDE.md`/`docs/05` con los comandos de despliegue reales. Verificación: los comandos copiados del documento levantan la instancia de cero. `[modelo: haiku]`
- [ ] 6.3 Review final del change: frontera de red verificada contra los escenarios (solo proxy publicado), coherencia de límites con `d14`, seed idempotente, restore probado con evidencia, smoke test en verde local y en CI, cero código de `core/`/`adapters/`/`app/` modificado. Verificación: checklist del reviewer en el PR con cada escenario de la spec marcado. `[modelo: opus]`
