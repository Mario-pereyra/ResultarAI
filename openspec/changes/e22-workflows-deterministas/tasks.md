# Tasks — e22-workflows-deterministas

## 1. Dominio puro del motor (core/orchestration/workflows/)

- [ ] 1.1 Definir `WorkflowDefinition` y `WorkflowStep` como modelos Pydantic `frozen=True` en `core/orchestration/workflows/definition.py`: `slug`, `version`, status publicado, pasos ordenados (tipos `llm | tool | hitl_pause`), inputs tipados declarados y archivos esperados (tipo, posición de inyección, presupuesto de extracción). Verificación: tests en `tests/core/` cubren "Paso de tipo no soportado es rechazado" y que una definición publicada no admite mutación (Pydantic rechaza asignación). `[modelo: opus]`
- [ ] 1.2 Implementar la máquina de estados pura de Paso (`pendiente → en curso → completado | fallido | esperando aprobación`) y de Corrida (`configurando → en ejecución → (esperando aprobación ⇄ en ejecución) → completada | fallida | cancelada`) como funciones de transición sin I/O en `core/orchestration/workflows/state.py`. Verificación: tests de tabla en `tests/core/` cubren "Transición inválida es rechazada", "Estado terminal es definitivo", "Pausa por HITL retorna solo a en ejecución" y "Un paso completado no se re-ejecuta". `[modelo: opus]`
- [ ] 1.3 Implementar `WorkflowRegistry` en `core/orchestration/workflows/registry.py`: registro explícito de definiciones desde código al arranque (fail-fast), consulta por status publicado, verificación de que cada paso `tool` referencia una Tool con `ToolManifest` activo en el Tool Registry (`c09`). Verificación: tests en `tests/core/` cubren "El registro expone solo Workflows con status publicado", "Paso de tipo Tool solo invoca Tools ya registradas" y "La versión activa no se edita en el lugar". `[modelo: opus]`
- [ ] 1.4 Test de determinismo del motor: dos Corridas de la misma `version` con inputs distintos exponen la misma lista de pasos, en el mismo orden e índice. Verificación: test en `tests/core/` cubre "Misma versión produce siempre los mismos pasos en el mismo orden" y "Publicar una versión nueva no altera la anterior". `[modelo: opus]`

## 2. Persistencia de Corridas y Pasos

- [ ] 2.1 Migración Alembic: tablas `workflow_runs` (id `WF-AAAA-NNNN`, `workflow_slug`, `workflow_version`, `owner_id`, inputs congelados JSONB, referencia a attachments) y `workflow_step_events` (transiciones append-only por paso: índice, tipo, estado, timestamps, referencia a Solicitud de aprobación cuando aplica), sobre la infraestructura de `b04`. Verificación: `alembic upgrade head` y `alembic downgrade -1` corren limpio; ningún camino de código hace `UPDATE` del estado (el estado actual es proyección de la última transición). `[modelo: sonnet]`
- [ ] 2.2 Repositorio de Corridas en `adapters/persistence_postgres/workflows.py` (crear Corrida con parámetros congelados, registrar transición de paso, proyectar estado actual, listar por dueño / todas para Admin, asociar artefacto de resultado). Verificación: contract test contra Postgres real cubre crear→transitar→proyectar y el filtrado por `owner_id`. `[modelo: sonnet]`

## 3. Ejecutor de Corridas (app/)

- [ ] 3.1 Caso de uso `start_workflow_run`: valida los inputs del formulario contra el schema de la `WorkflowDefinition`, procesa los archivos esperados vía el pipeline de `d14-attachments` (tipo/posición/presupuesto fijados por la definición, sin control del usuario) y crea la Corrida en `configurando` → `en ejecución`. Verificación: test cubre "Formulario válido crea la Corrida en configurando", "Archivo fuera del tipo declarado es rechazado" y "El usuario no puede cambiar la posición ni el presupuesto de extracción". `[modelo: opus]`
- [ ] 3.2 Ejecutor secuencial de pasos en `app/use_cases/workflows/executor.py`: recorre los pasos en el orden fijo de la definición; paso `llm` evalúa cuota (`d16`) antes de emitir vía `LLMPort` y registra consumo; paso `tool` construye el `ActionRequest`, consulta `PolicyPort` y solo ejecuta con `allow`; cada transición emite evento vía `TracePort` (`b07`). Verificación: test con dobles de ports cubre "Cuota insuficiente antes de un paso LLM detiene la Corrida" (estado `QUOTA` accionable), "Cuota suficiente permite continuar" y "Una Corrida es reconstruible por su traza". `[modelo: opus]`
- [ ] 3.3 Integración de la pausa HITL: el paso `hitl_pause` invoca la Tool de escritura simulada, recibe `PolicyDecision.effect = escalate_hitl`, crea la Solicitud de aprobación de `d17` (misma entidad, sin duplicarla), guarda solo su referencia en el Paso y pasa Corrida y Paso a `esperando aprobación`; al resolverse (aprobada/rechazada/expirada) reanuda, falla o retira según la decisión. Verificación: test cubre "El paso de escritura declara la decisión escalate_hitl del Policy Gate", "Aprobación reanuda la Corrida en el paso siguiente" y "Expiración de la tarjeta es rechazo auditado y falla el paso". `[modelo: opus]`
- [ ] 3.4 Caso de uso `cancel_workflow_run` (dueño o Admin): detiene el paso en curso, conserva los pasos `completado` tal cual (nada se revierte), retira la Solicitud de aprobación pendiente si la hay y emite `AuditEvent`. Verificación: test cubre "Cancelar detiene sin revertir lo ya ejecutado", "Cancelar con pausa HITL pendiente retira la solicitud" y "Cancelación queda auditada". `[modelo: sonnet]`
- [ ] 3.5 Generación del artefacto de resultado server-side (xlsx vía openpyxl / md) asociado a la Corrida al completar, con trazabilidad (quién, cuándo, inputs; costo solo Técnico/Admin). Verificación: test cubre "El resultado queda disponible tras completar" y "Corrida fallida no genera resultado completo". `[modelo: sonnet]`
- [ ] 3.6 Emisión de notificaciones al alcanzar estado terminal vía el contrato de `d12` (tipos ya registrados de workflow terminado/fallido/cancelado) con deep link al resultado o detalle. Verificación: test cubre "Notificación al completar con la vista cerrada" y "Notificación distingue el estado terminal alcanzado". `[modelo: sonnet]`

## 4. Endpoints y streaming (app/api)

- [ ] 4.1 Endpoints REST: `GET /workflows` (listado filtrado por matriz workflow×rol, reutilizando el contrato de visibilidad de `d15` con default Técnico+Admin), `POST /workflows/{slug}/runs` (crear Corrida), `GET /workflows/runs/{id}` (detalle con estado por paso), `POST /workflows/runs/{id}/cancel`. Verificación: tests cubren "Listado respeta la visibilidad por rol", "Rol sin workflows habilitados no ve la sección" (payload de navegación) y ownership en detalle/cancelación. `[modelo: sonnet]`
- [ ] 4.2 Streaming de progreso de una Corrida por el mismo transporte en vivo de `d13` (SSE/WSS): eventos de transición de paso; reconexión re-sincroniza con el estado completo actual, la Corrida sigue en servidor. Verificación: test cubre "El estado de cada paso se actualiza en vivo" y "La Corrida continúa si la vista se desconecta". `[modelo: sonnet]`
- [ ] 4.3 Endpoints de historial: `GET /workflows/runs` (propias; Admin ve todas con filtro de usuario; filtros por workflow/estado/fechas; paginación clásica) y `GET /workflows/runs/{id}/result` (descarga del artefacto, auditada). Verificación: tests cubren "Usuario no-Admin solo ve sus propias Corridas" y "Admin ve todas las Corridas". `[modelo: sonnet]`

## 5. Frontend — vistas 15 a 19

- [ ] 5.1 Vista 15 (lista): grid de cards con nombre, qué produce, pasos, requisitos, historial reciente propio y CTA "Configurar"; costo estimado y `slug@version` solo Técnico/Admin; estados carga/vacío/error según `design/VISTAS/04-workflows.md` §1; entrada de sidebar oculta sin workflows habilitados. Verificación: cubre "Funcional no ve costo estimado ni versión" y "Técnico ve la capa técnica completa". `[modelo: sonnet]`
- [ ] 5.2 Vista 16 (formulario): renderer genérico de inputs a partir del schema serializado de la definición (texto, número, selección, archivo), validación inline al blur, CTA deshabilitado con motivo visible enumerando lo que falta, panel lateral de resumen (pasos previstos, costo estimado T/A, barra de cuota compacta), error `QUOTA` accionable pre-ejecución. Verificación: cubre "Ejecutar deshabilitado con motivo visible" y redirección a la vista 17 al crear la Corrida. `[modelo: sonnet]`
- [ ] 5.3 Zona de archivo esperado del formulario: arrastre + "Elegir archivo", chip con estados (subiendo/extrayendo, listo, error con motivo accionable), tipos y tamaño limitados a lo que declara la definición. Verificación: cubre "Archivo fuera del tipo declarado es rechazado" desde la UI. `[modelo: sonnet]`
- [ ] 5.4 Vista 17 (ejecución en vivo): timeline `.wf` con estado por paso en vivo, progreso global "Paso n de N", log colapsable por paso (detalle técnico solo T/A), variante pausado-HITL con enlace a la Tarjeta HITL de `d17` (sin duplicarla), banner "reconectando…" ante caída del stream, modal de cancelación con confirmación y CTA "Ver resultado" al completar (sin auto-redirección). Verificación: cubre reconexión re-sincronizada y el enlace a la tarjeta en pausa. `[modelo: sonnet]`
- [ ] 5.5 Vista 18 (resultado): encabezado con estado + id + acciones de descarga (xlsx/md) y "Re-ejecutar", cuerpo con el informe generado, bloque de trazabilidad (quién/cuándo/inputs/workflow@version; costo y tokens solo T/A), variante de Corrida fallida con enlace al paso fallido. Verificación: cubre descarga asociada a la Corrida y la variante fallida. `[modelo: sonnet]`
- [ ] 5.6 Vista 19 (historial): filtros (workflow/estado/fechas; usuario solo Admin) en URL compartible, tabla con id/estado/duración/fecha (columna costo solo T/A), acciones "Ver" y "Re-ejecutar" (solo Corridas terminadas), modal de re-ejecución con inputs congelados y advertencia si la versión activa cambió, paginación clásica. Verificación: cubre "Re-ejecutar no sobrescribe la Corrida original" y "Re-ejecutar con versión de workflow cambiada lo advierte". `[modelo: sonnet]`
- [ ] 5.7 Externalizar textos de las 5 vistas al catálogo i18n (estados de Corrida/Paso como glosario cerrado, plurales ICU, ids/códigos no traducibles) y accesibilidad AA (timeline como `<ol>` con `aria-label` por paso, `aria-live` en transiciones, targets ≥44 px en móvil). Verificación: claves presentes en catálogo ES; auditoría axe sin violaciones AA en las 5 vistas. `[modelo: sonnet]`

## 6. Workflow de ejemplo de fábrica

- [ ] 6.1 Declarar `informe-adjunto@1.0` ("Generar informe a partir de un adjunto") en `core/orchestration/workflows/factory.py`: paso 1 lectura/extracción del adjunto, paso 2 llamada LLM que redacta el informe, paso 3 Tool de escritura simulada del MCP Server de ejemplo de `c09` (pausa HITL); registrado como publicado en el `WorkflowRegistry`, visible por default para Técnico+Admin. Verificación: la definición carga fail-fast al arranque y aparece en `GET /workflows`; test en `tests/core/` valida sus 3 pasos y tipos. `[modelo: sonnet]`
- [ ] 6.2 Test E2E del ciclo completo del ejemplo (con dobles de LLM y MCP, sin red externa): formulario → extracción → paso LLM (cuota evaluada) → pausa HITL → aprobación → resultado descargable → notificación → visible en historial. Verificación: cubre "El workflow de ejemplo corre de punta a punta con una pausa HITL" y "Rechazar la pausa dentro del ejemplo termina la Corrida como fallida" — el criterio "hecho cuando" del roadmap. `[modelo: sonnet]`
- [ ] 6.3 Fixture/seed de desarrollo: una Corrida completada del ejemplo con su artefacto md, para poblar historial y vista de resultado en local. Verificación: el seed corre sin error y la Corrida aparece en el historial del usuario semilla. `[modelo: haiku]`

## 7. Cierre

- [ ] 7.1 Review final del change: consistencia proposal↔specs↔design↔tasks, regla de dependencia intacta (`core/orchestration/workflows/` sin frameworks, verificado por `lint-imports`), pausa HITL sin duplicar la tarjeta de `d17`, determinismo verificado, y lista de términos nuevos para `docs/03-glosario-dominio.md` (Workflow, Corrida, Paso, Workflow Registry) reportada para su incorporación. Verificación: checklist del reviewer en el PR; suite completa en verde. `[modelo: opus]`
