# Tasks — d21-builders

## 1. Modelo compartido de borradores y versiones

- [ ] 1.1 Definir el invariante "versión hornea toolset/skillset fijos": contrato de snapshot inmutable de toolset+skillset al activar una versión de agente y regla de stickiness (una sesión conserva su versión durante toda su vida). Verificación: test de invariante — activar `@n+1` con toolset distinto no altera el toolset de una sesión abierta sobre `@n`. `[modelo: opus]`
- [ ] 1.2 Implementar el modelo compartido de borrador (un borrador por entidad, estados, lock optimista `EDIT_LOCKED`, autosave, `AuditEvent` por transición) en `app/builders/` + tabla y migración Alembic. Verificación: tests — segundo borrador rechazado; segundo Admin entra en solo lectura; cada transición emite `AuditEvent`. `[modelo: sonnet]`
- [ ] 1.3 Implementar la precedencia DB-sobre-YAML para entidades con versión en Postgres (el YAML de fábrica solo siembra) y el espejo Git en cada publicación, con validación en CI de que el espejo parsea contra los schemas de `a02`. Verificación: publicar deja snapshot en `manifests/`; editar el snapshot a mano no afecta el runtime. `[modelo: sonnet]`

## 2. Agent Builder — backend

- [ ] 2.1 Definir el contrato de la API de borradores de agente SIN campo de prefijo (el header cache-safe lo compone la plataforma y no existe en el payload editable); componer el prompt final = prefijo de plataforma + secciones editables. Verificación: test — un payload que intente incluir/alterar el prefijo se rechaza; el prompt compuesto siempre arranca con el prefijo intacto. `[modelo: opus]`
- [ ] 2.2 Implementar casos de uso del wizard: Paso 1 (identidad + matriz de visibilidad vía contrato de escritura de `d20`, con regla encadenada catálogo→sesiones), Paso 2 (versión de prompt vinculada al Registro de Prompts de `d20`), Paso 3 (toolset solo desde Tools `active` de `c09`, clasificación/HITL solo lectura; perfil de modelo y escalación de `b05`). Verificación: tests por paso — tool `deprecated` no seleccionable; HITL de escritura sin control de edición; `escalation.enabled` persiste en el manifiesto. `[modelo: sonnet]`
- [ ] 2.3 Implementar "Activar versión": validación 4 pasos + invocación del gate pluggable de `d20` (placeholder: advertencia visible, no bloqueo) + transiciones de `status` (`active`/`deprecated`) + `AuditEvent` con el resultado del gate. Verificación: test — activar sin evals registra advertencia en el `AuditEvent`; la versión previa queda `deprecated`; el agente aparece en el catálogo (`d15`) según su matriz. `[modelo: opus]`

## 3. Skills Builder — backend

- [ ] 3.1 Implementar la validación en vivo del frontmatter reutilizando las reglas de validación de paquetes de `c08` (name 1–64, minúsculas/dígitos/guiones, description obligatoria, name = slug) como endpoint de validación por campo. Verificación: tests con la tabla de casos inválidos de `c08` — cada uno devuelve la regla violada; frontmatter inválido ⇒ publicación imposible también por API. `[modelo: sonnet]`
- [ ] 3.2 Implementar el gate de conteo ≥3 casos de eval declarados (id, descripción, criterio) sin ejecución (modo placeholder), casos versionados con la skill como dataset para `e25`; re-bloqueo en vivo si el conteo cae bajo 3 en un borrador. Verificación: tests — publicar con 2/3 rechazado por API; con 3/3 publica sin ejecutar nada; versión publicada no permite quitar casos. `[modelo: opus]`
- [ ] 3.3 Implementar publicar/archivar skill: congelar versión inmutable, materializar paquete `SKILL.md` + `SkillManifest` que pasa la validación de `c08`, changelog append-only, archivar-con-advertencia sin tocar versiones de agente horneadas. Verificación: test — el paquete publicado valida con el loader de `c08`; archivar no cambia el skillset de una versión activa de agente. `[modelo: sonnet]`
- [ ] 3.4 Implementar el Registro de skills (consulta): slug, versión, owner informativo, agentes que la usan (derivado de `enabled_skills` activos), estado y conteo de casos; solo lectura para Técnico. Verificación: test de contrato — Técnico lista sin acciones de escritura; owner Técnico no puede editar. `[modelo: sonnet]`

## 4. Propuestas guiadas y sandbox

- [ ] 4.1 Implementar la entidad `proposal` (template fijo + secciones editables + toolset filtrado por matriz rol×tool de `d20`, validado en servidor), estados pendiente/aprobada/rechazada, conversión a borrador al aprobar, motivo obligatorio al rechazar, notificaciones vía `d12` y `AuditEvent` en cada decisión. Verificación: tests — propuesta pendiente invisible fuera de la bandeja del Admin; tool no permitida por rol rechazada en servidor; aprobar crea borrador sujeto al gate normal. `[modelo: sonnet]`
- [ ] 4.2 Implementar el sandbox: construcciones visibles solo para su creador, selector limitado a Tools `read`, ejecución por el mismo runtime con contexto `sandbox` sin ningún `allow` de escritura en Policies. Verificación: test — invocación de tool de escritura desde sandbox ⇒ decisión `deny` del Policy Gate registrada en el Audit Log; la construcción no aparece en el catálogo de otros usuarios. `[modelo: opus]`
- [ ] 4.3 Implementar la promoción sandbox→catálogo (solo Admin): conversión a borrador del builder + gate completo sin atajos (incluye ≥3 casos si es skill). Verificación: test — promover sin pasar el gate no publica; roles no-Admin no tienen la acción. `[modelo: sonnet]`

## 5. Frontend — vistas 41 y 42

- [ ] 5.1 Portar textos, labels y estados de las vistas 41/42 desde `design/VISTAS/09-builders.md` al catálogo i18n (es-BO, claves; slugs/códigos/versiones sin traducir; mensajes ICU del gate n/3). Verificación: cero strings hardcodeados en los componentes de builders. `[modelo: haiku]`
- [ ] 5.2 Implementar la vista 41 (Agent Builder): stepper de 4 pasos con estados de validación, encabezado con borrador/base/autosave, bloque de prefijo con candado, allowlist de tools, matriz de visibilidad con regla encadenada, paso Evals con advertencia placeholder, modal de activación, estados carga/vacío/error/degradado (`REGISTRY_OFFLINE`, `SAVE_FAILED`, `EDIT_LOCKED`) y gate desktop-only en <768px. Verificación: flujo crear→editar→activar completo contra el backend en entorno local; axe sin violaciones AA. `[modelo: sonnet]`
- [ ] 5.3 Implementar la vista 42 (Skills Builder): registro (tabla), banda de recordatorio de horneado, editor con validación en vivo del frontmatter, barra de gate n/3 con mensaje exacto del design, tabla de casos, agentes destino con CTA al builder de agentes, changelog timeline, toast de publicación con recordatorio accionable, estados y gate móvil. Verificación: publicar con 2/3 imposible desde la UI y la API; con 3/3 el toast enlaza al Agent Builder; axe sin violaciones AA. `[modelo: sonnet]`
- [ ] 5.4 Implementar las pantallas de propuesta guiada (Técnico: formulario con template fijo y toolset acotado; Admin: bandeja aprobar/rechazar con motivo) y del sandbox (constructor no-code sin tools de escritura, listado propio). Verificación: flujo Técnico propone → Admin aprueba → borrador visible solo entonces; selector sandbox sin ninguna tool de escritura. `[modelo: sonnet]`

## 6. Cierre

- [ ] 6.1 Tests E2E de los cuatro escenarios clave: skill con frontmatter inválido no publicable; agente creado aparece en catálogo tras el gate; propuesta de Técnico invisible hasta aprobación del Admin; sandbox jamás ofrece escrituras y el Policy Gate deniega el intento. Verificación: suite E2E en verde en CI. `[modelo: sonnet]`
- [ ] 6.2 Review final del change: invariantes duros no presentes en ningún payload editable (prefijo, HITL, clasificación), gates sin bypass por API, auditoría completa, consistencia con `d20`/`c08`/`c09` y con el glosario; `core/` intacto (import-linter en verde). Verificación: checklist del reviewer en el PR. `[modelo: opus]`
